# Web framework
Flask==2.3.2
serverless-wsgi==3.0.0
# MongoDB driver
pymongo

# LangChain (core version handled automatically)
langchain
# langchain-community is optional, can install if needed: pip install langchain-community

# HuggingFace integration
huggingface_hub
transformers
accelerate

# Structured output / schema
pydantic
python-dotenv

# Optional: for HTTP requests
requests
